# SN41 Discord

## Main-Bot
Environment options:

| Option           | Needed | Description               | Default |
|------------------|--------|---------------------------|---------|
| `DISCORD__TOKEN` | ✅      | API Token used by the Bot |         |

For development, you can use DotNet UserSecrets:
```shell
dotnet user-secrets set "Discord:Token" "your-actual-token-here"
```
